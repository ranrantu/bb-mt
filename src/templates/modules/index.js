/**
 * @file
 * @date ~datetime~
 * @authors ~username~ (~email~)
 * @description
 */
import moduleName from './moduleName.vue';
import * as constants from './moduleNameConstants';

export default {
  name: constants.NAME,
  path: constants.PATH,
  component: moduleName,
  meta: {
    title: constants.TITLE,
    val_cid: constants.VAL_CID
  }
};
